transaction-support {
  match {
    // Match pattern
    Disease (this)
  }

  confirm-on-abandon {
    // Message to show prior to abandoning a transaction
    message ("Are you sure you want to leave?")
    on-retain {
      goal {
        SearchDisease @context (Continuation)
      }
    }
  }
}
