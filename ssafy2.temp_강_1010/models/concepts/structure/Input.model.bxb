structure (Input) {
  description (intput structure..)
  property (inputPart) {
    type (InputPart)
    min (Required) max (Many)
  }

  property (inputSymptom) {
    type (InputSymptom)
    min (Required) max (Many)
  }

  // features {
  //   transaction
  // }
}
